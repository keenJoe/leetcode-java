148. Sort List
1. 创建一个listnode存放排序后的链表，空间复杂度为O(n)
2. while循环遍历head链表，得到每一个节点
3. 将每一个节点和listnode从头比较，直到找到合适的位置插入，这也是一个while循环

但是这样时间复杂度就会变大。
刚才自己也说过，链表、数组和字符串它们三者有一个相似点：数据结构，都是一条链子。
所以数组排序的方式应该也可以应用到链表上。
看来要把排序的算法过一遍了。
使用不同的算法实现sort list。