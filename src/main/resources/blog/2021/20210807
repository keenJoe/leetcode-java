关于binary tree的前序遍历
前序遍历的次序是：root，左节点，右节点。

看了那么多的solution，自己却不知道如何把答案用文字的形式描述出来。

1. 将两棵子树分别遍历
2. 使用stack存储已经访问的节点，形成一个访问路径。然后一直向左遍历，如果左节点为空，则取回上一个root节点的右节点，再开始从左遍历。
3. 使用stack存储已经访问的节点，形成一个访问路径。先记录root节点的值。因为stack的特点是后进先出，
   且前序遍历输出时左节点的值在右节点之前，所以先将root的右节点放进stack中，然后再判断左节点。
   这样，如果左节点为空，那么直接pop出一个节点，这个节点就是右节点。
   然后再循环以上步骤。